[package]
name = "neko-auth"
version = "0.1.0"
edition = "2024"

[dependencies]
worker = { version = "0", features = ["http"] }
axum = { version = "0", default-features = false }
time = { version = "0", default-features = false }
